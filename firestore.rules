rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Anyone can see the deals (Public)
      allow read: if true;
      
      // Only logged-in users can write (Subscribers & Admin)
      allow write: if request.auth != null;
    }
  }
}